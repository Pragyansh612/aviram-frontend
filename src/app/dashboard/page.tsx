import DashboardApp from "@/components/dashboard/App";

export const metadata = {
  title: "Aviram — Command Center",
  description: "Autonomous job search dashboard",
};

export default function DashboardPage() {
  return <DashboardApp />;
}